# -*- coding: utf-8 -*-
"""Mirage —— 多平台反检测加固 + 拟人互动引擎工具集（仅供学习研究）。

本目录经 pyproject 的 package-dir 映射为 `mirage` 包：
  - pip 安装后：`import mirage.apply_hardening` / `mirage` 命令可用；
  - 同时保留扁平 `scripts/` 结构，供 Claude Code skill 识别与直接 `python scripts/xxx.py` 运行。
"""
__version__ = "0.6.0"
