# -*- coding: utf-8 -*-
"""Mirage  --  multi-platform anti-detection hardening + human-like interaction engine toolset (for study/research only) .

This directory is mapped to the `mirage` package via pyproject's package-dir:
  - after pip install: `import mirage.apply_hardening` / `mirage` command available;
  - while keeping the flat `scripts/` structure, for Claude Code skill recognition and direct `python scripts/xxx.py` execution.
"""
__version__ = "0.6.0"
