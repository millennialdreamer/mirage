# Mirage — dependency notes
#
# Prefer `pip install -e ".[browser]"` (see pyproject.toml). This file is kept for
# environments that still expect a requirements.txt.
#
# Zero third-party dependencies (standard library only):
#   apply_hardening.py, safe_profile.py, soft_ban_radar.py, device_profile.py,
#   interaction_policy.py, cli.py
#
# Needs a browser driver:
#   verify_stealth.py          launches a browser to verify stealth.min.js took effect
#   fingerprint_benchmark.py   --detect-url / --botd checks
#   human_behavior.py          Bézier mouse paths, injected into the target core.py
#   human_interaction.py       interaction engine (like / follow / collect / comment)
#   mirage_loop.py             main human-like loop
#
# patchright is a drop-in Playwright replacement that also closes the CDP
# Runtime.Enable leak — injected scripts cannot reach that layer. Prefer it.
#   pip install patchright && patchright install chromium
patchright>=1.40

# Fallback if you must use stock Playwright (leaves the CDP leak in place):
# playwright>=1.40
